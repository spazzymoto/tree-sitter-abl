================
create statement
================

CREATE ttTest NO-ERROR.

---

(program
  (create_statement
    (kwCREATE)
    (identifier)
    (kwNO_ERROR)))

========================
create statement with db
========================

CREATE database.table.

---

(program
  (create_statement
    (kwCREATE)
    (scoped_identifier
      (identifier)
      (identifier))))
